"""
Script de treino dos modelos de classificação de fraude.

Este arquivo é o ponto de partida da PARTE 2 do trabalho (a fazer em
seguida): carregar o dataset (Kaggle Credit Card Fraud), tratar o
desbalanceamento de classes, treinar e comparar os três algoritmos
(Regressão Logística, Árvore de Decisão e Random Forest), calcular as
métricas (precisão, recall, F1, AUC-ROC) e salvar o modelo escolhido em
ml/modelos_salvos/modelo_atual.pkl no formato esperado pelo
services/ml_service.py:

    {
        "modelo": <objeto do modelo treinado>,
        "colunas": [<lista de nomes das colunas de entrada, na ordem>],
        "versao": "<identificador da versão, ex: 'rf_v1_2026-08-27'>",
    }

Deixado propositalmente vazio nesta etapa (esqueleto do projeto) —
será implementado na próxima parte.
"""

if __name__ == "__main__":
    raise NotImplementedError(
        "Script de treino ainda não implementado — próxima etapa do trabalho."
    )
